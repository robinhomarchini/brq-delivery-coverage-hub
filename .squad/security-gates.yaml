gates:
  local:
    - npm run validate
    - npm run build
  persistence:
    - npm run smoke:critical
  database:
    - npm run db:migrations:check
  security:
    - npm run test:security
rules:
  preserve_rls: true
  preserve_forward_migrations: true
  production_local_fallback: false
  secrets_in_git: false
